name: boolean
description: Extension functions on strings and numbers to convert them to bools.
version: 1.0.1
repository: https://github.com/mjstel/boolean

environment:
  sdk: ">=2.10.0 <3.0.0"

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
